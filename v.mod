Module {
	name: 'kitchen'
	description: '✨✨✨ Kitchen ✨✨✨ runs a command upon file modifications (transfers the modified watched file, with -scp). With one or more -p options, post commands are run.\n\nEssentially, a watchexec program that runs a command on source or text file modification. Ignores common binary extensions and dirs.'
	version: '1.0.6'
	license: 'ISC'
	dependencies: [
		'https://github.com/scriptmaster/vmon'
	]
}
